# Implementation checkpoints

Base: `2b27076f32bd775c55695c41bfb6a60ff0b83256` (PR #26). Worktree: isolated from the original checkout.

A. Define permissions, then PostgreSQL schema/RLS and versioned command contracts. Use Supabase authentication/private objects, restricted SQL transactions, existing Solve domain rules, explicit import provenance, private editor and recoverable files. Verify actual PostgreSQL policies, revisions, approval dependencies, records/bytes restore, and available browser flows. Commit the checkpoint and measured evidence.

B. Add Stripe test-only Team checkout, portal, verified webhooks/reconciliation, entitlement races, outbox jobs and lifecycle UI. Verify billing and regressions. Finish operational/trust documentation and a reviewable draft PR.

No production migration, deployment, real invitation, service purchase, live billing, Nostr relay, or agent execution is authorized.
