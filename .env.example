LEAD_WEBHOOK_URL=
LEAD_WEBHOOK_SECRET=
NEXT_PUBLIC_SITE_URL=https://www.loopsignal.co
NEXT_PUBLIC_GSC_VERIFICATION=

# Private company workspace — server only, never NEXT_PUBLIC_*.
# For local development, set NEXT_PUBLIC_SITE_URL=http://localhost:3117 above.
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SERVICE_ROLE_KEY=
# A restricted NOINHERIT login granted only loop_app; never postgres/service_role.
# Hosted connections MUST use sslmode=verify-full and a valid CA chain.
COMPANY_DATABASE_URL=
# Invitation delivery is synthetic-only and restricted to a local sink in this milestone.
COMPANY_SMTP_HOST=127.0.0.1
COMPANY_SMTP_PORT=54325
